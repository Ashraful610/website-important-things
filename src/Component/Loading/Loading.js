import React from 'react';
import loadingGif from '../../image/loading-2.gif'

const Loading = () => {
    return (
        <div>
            <img src={loadingGif} alt="" />
        </div>
    );
};

export default Loading;